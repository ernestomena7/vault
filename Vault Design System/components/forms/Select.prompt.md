Single-select dropdown — destination folder, naming convention template, approver, region.

```jsx
<Select label="Naming convention" options={[{value:'v1',label:'{project}_{date}_v{n}'},{value:'v2',label:'{date}_{project}'}]} />
```
