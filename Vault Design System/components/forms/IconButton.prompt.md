A square icon-only button, for row actions and toolbars (more menu, download, delete).

```jsx
<IconButton icon="more-horizontal" aria-label="More actions" />
```

Defaults to `ghost` (blends into rows); use `secondary` for a standalone toolbar button.
