Drag-and-drop zone for starting a video upload. Drives the whole upload flow: idle → dragging → uploading (with progress) → error (with retry).

```jsx
<FileDropzone state="uploading" fileName="onboarding_v3.mp4" progress={64} />
```
