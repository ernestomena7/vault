Primary left navigation rail for the app shell.

```jsx
<Sidebar items={[{value:'dash',label:'Dashboard',icon:'layout-dashboard'},{value:'up',label:'Uploads',icon:'upload-cloud'}]} activeValue="dash" onChange={setNav} footer={<UserMenu/>} />
```
